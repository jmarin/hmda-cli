pub struct AutomatedUnderwritingSystemResult {
    pub aus_result1: u8,
    pub aus_result2: u8,
    pub aus_result3: u8,
    pub aus_result4: u8,
    pub aus_result5: u8,
    pub other_aus_result: String,
}
